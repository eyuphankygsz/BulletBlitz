using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainMenu : MonoBehaviour
{
    public GameObject[] TumMenuler;

    int arabaSayisi = 2;

    public void MenuAcma(GameObject menu)
    {
        Debug.Log(arabaSayisi);

        for (int i = 0; i < TumMenuler.Length; i++)
        {

        }
    }








    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
